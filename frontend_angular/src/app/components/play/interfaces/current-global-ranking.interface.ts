import {CurrentQuestionRankingInterface} from "./current-question-ranking.interface";

export interface CurrentGlobalRankingInterface{
  globalPoints: number;
  globalRanking: number;
  globalTotalPlayers: number;
}
